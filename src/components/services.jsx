import React from "react";

export const Services = (props) => {
  return (
    <div id="services" className="text-center">
      <div className="container">
        <div className="section-title">
          <h2>We Don’t Hire Average</h2>
          <p>
            We’re not your typical call center — and we’re not looking for typical agents.
If you’ve got the drive, the hunger, and the skills, there’s a seat at the table. Whether you're just getting started or already running the show somewhere else, we’ve got room for talent that plays to win.
          </p>
        </div>
        <div className="row">
          {props.data
            ? props.data.map((d, i) => (
                <div key={`${d.name}-${i}`} className="col-md-4">
                  {" "}
                  <i className={d.icon}></i>
                  <div className="service-desc">
                    <h3>{d.name}</h3>
                    <p>{d.text}</p>
                  </div>
                </div>
              ))
            : "loading"}
        </div>
      </div>
    </div>
  );
};
